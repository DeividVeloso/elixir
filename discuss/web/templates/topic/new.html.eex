<%= form_for @changeset, topic_path(@conn, :create), fn formObject -> %>
  <div class="form-group">
    <%= text_input formObject, :title, placeholder: "Title", class: "form-control" %>
    <%= error_tag formObject, :title %>
  </div>
  <%= submit "Save Topic", class: "btn btn-primary" %>
<% end %>